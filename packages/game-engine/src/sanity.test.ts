import { describe, expect, it } from "vitest";

// Placeholder test proving the Vitest setup works end-to-end.
// Delete this once real engine tests exist (Step 1, task 12).
describe("sanity", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
